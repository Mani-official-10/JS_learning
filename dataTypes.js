// "use strict"; ----> use new version of the js for the js file.

alert( 3 + 3);
// alert is not allwed in node.js environment.

console.log(3+
    3);
    // code readability is always on priority.

    // ECMA script is introduced to register a standard to write the javascript in all over the world.
    // ECMA script writes the standards of javascript.

    // DATA TYPES

    // A) string:
    let name = "manish";
    // B) number
    let Age = 31;
    // C) Boolen
    let isLoggedIn = false;

    // D) bigInt

    // E)  null -> standalone value
    let temp = null;

    // F) undefined -> value not assigned.

    let value;

    // symbol -> unique

    // objects;
    